# Current Issues

No open architecture-blocking issue.

Use this file only when an unresolved failure cannot be fixed inside the active feature spec without changing architecture, authority, privacy, SSOT ownership, repo topology, or scope.

When needed, record:
- observed failure and command/output;
- affected subsystem;
- why the current spec cannot resolve it safely;
- smallest proposed change;
- verification plan;
- human decision required.

Do not use this file for ordinary local RED→GREEN fixes already inside the approved feature unit.