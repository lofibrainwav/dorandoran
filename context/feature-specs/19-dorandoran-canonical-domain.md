# Unit 19 — DoranDoran Canonical Website

- Canonical public website: `https://dorandoran.link`.
- Next metadata must emit that canonical origin rather than a Vercel preview hostname.
- User-facing Family OS brand is `DoranDoran · Family OS`; internal core/module names remain unchanged.
- Domain attachment/DNS is deployment configuration, not application business logic.
- Connecting the public domain must not weaken the existing rule that private `/family` sources are disabled on Vercel.
